# Copy this file to `.env` and fill in values from Netlify (one-time setup).
# User settings → Applications → Personal access tokens
NETLIFY_AUTH_TOKEN=your_token_here
# Site settings → Site details → Site ID (also shown in the site URL slug)
NETLIFY_SITE_ID=your_site_id_here
